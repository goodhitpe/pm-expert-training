# Week 14: 데이터베이스 (Database)

## 📝 학습 목표
- 데이터베이스의 개념과 필요성 이해
- 관계형 데이터베이스와 SQL 기초 학습
- 데이터 모델링 및 정규화 이해
- NoSQL 데이터베이스의 특성 파악

## 📚 주요 내용

### 1. 데이터베이스 개요

#### 1.1 데이터베이스란?
- **정의**: 조직화되고 구조화된 데이터의 집합
- **DBMS (Database Management System)**: 데이터베이스를 관리하는 소프트웨어
- **목적**:
  - 데이터 중앙 관리
  - 데이터 무결성 보장
  - 동시성 제어
  - 보안 및 백업

#### 1.2 파일 시스템 vs 데이터베이스

| 구분 | 파일 시스템 | 데이터베이스 |
|------|------------|-------------|
| 데이터 중복 | 많음 | 최소화 |
| 데이터 일관성 | 낮음 | 높음 |
| 동시 접근 | 제한적 | 지원 |
| 보안 | 약함 | 강력 |
| 백업/복구 | 어려움 | 용이 |
| 데이터 관계 | 없음 | 명확 |

#### 1.3 데이터베이스 종류
- **관계형 데이터베이스 (RDBMS)**: MySQL, PostgreSQL, Oracle, SQL Server
- **NoSQL 데이터베이스**: MongoDB, Redis, Cassandra
- **인메모리 데이터베이스**: Redis, Memcached
- **그래프 데이터베이스**: Neo4j
- **시계열 데이터베이스**: InfluxDB

### 2. 관계형 데이터베이스 (RDBMS)

#### 2.1 관계형 모델
- **테이블 (Table)**: 데이터를 행과 열로 구성
- **행 (Row/Tuple)**: 레코드, 개별 데이터 항목
- **열 (Column/Attribute)**: 속성, 데이터 필드
- **기본 키 (Primary Key)**: 각 행을 고유하게 식별
- **외래 키 (Foreign Key)**: 다른 테이블과의 관계

**테이블 예시: Users**
```
┌────┬──────────┬─────────────────┬──────┐
│ ID │ Name     │ Email           │ Age  │
├────┼──────────┼─────────────────┼──────┤
│ 1  │ 홍길동    │ hong@email.com  │ 30   │
│ 2  │ 김철수    │ kim@email.com   │ 25   │
│ 3  │ 이영희    │ lee@email.com   │ 28   │
└────┴──────────┴─────────────────┴──────┘
```

#### 2.2 관계의 종류
- **1:1 관계**: 사용자 ↔ 프로필
- **1:N 관계**: 부서 ↔ 직원
- **N:M 관계**: 학생 ↔ 강의 (중간 테이블 필요)

#### 2.3 무결성 제약조건
- **개체 무결성**: 기본 키는 NULL이 될 수 없음
- **참조 무결성**: 외래 키는 참조 테이블의 기본 키 값이어야 함
- **도메인 무결성**: 속성 값은 정의된 도메인에 속해야 함

### 3. SQL (Structured Query Language)

#### 3.1 SQL 종류
- **DDL (Data Definition Language)**: CREATE, ALTER, DROP
- **DML (Data Manipulation Language)**: SELECT, INSERT, UPDATE, DELETE
- **DCL (Data Control Language)**: GRANT, REVOKE
- **TCL (Transaction Control Language)**: COMMIT, ROLLBACK

#### 3.2 기본 SQL 문법

**데이터베이스 및 테이블 생성 (DDL):**
```sql
-- 데이터베이스 생성
CREATE DATABASE company_db;

-- 테이블 생성
CREATE TABLE employees (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) UNIQUE,
    department VARCHAR(50),
    salary DECIMAL(10, 2),
    hire_date DATE
);
```

**데이터 조회 (DML - SELECT):**
```sql
-- 전체 데이터 조회
SELECT * FROM employees;

-- 특정 컬럼 조회
SELECT name, email FROM employees;

-- 조건 조회 (WHERE)
SELECT * FROM employees WHERE department = 'IT';

-- 정렬 (ORDER BY)
SELECT * FROM employees ORDER BY salary DESC;

-- 제한 (LIMIT)
SELECT * FROM employees LIMIT 10;
```

**데이터 삽입 (DML - INSERT):**
```sql
INSERT INTO employees (name, email, department, salary, hire_date)
VALUES ('홍길동', 'hong@company.com', 'IT', 50000.00, '2024-01-01');
```

**데이터 수정 (DML - UPDATE):**
```sql
UPDATE employees
SET salary = 55000.00
WHERE id = 1;
```

**데이터 삭제 (DML - DELETE):**
```sql
DELETE FROM employees WHERE id = 1;
```

#### 3.3 고급 SQL

**조인 (JOIN):**
```sql
-- INNER JOIN
SELECT e.name, d.department_name
FROM employees e
INNER JOIN departments d ON e.department_id = d.id;

-- LEFT JOIN
SELECT e.name, d.department_name
FROM employees e
LEFT JOIN departments d ON e.department_id = d.id;
```

**집계 함수 (Aggregate Functions):**
```sql
-- COUNT, SUM, AVG, MAX, MIN
SELECT 
    department,
    COUNT(*) as employee_count,
    AVG(salary) as avg_salary,
    MAX(salary) as max_salary
FROM employees
GROUP BY department
HAVING COUNT(*) > 5;
```

**서브쿼리 (Subquery):**
```sql
-- WHERE 절에서 서브쿼리
SELECT name, salary
FROM employees
WHERE salary > (SELECT AVG(salary) FROM employees);

-- FROM 절에서 서브쿼리
SELECT dept, avg_sal
FROM (
    SELECT department as dept, AVG(salary) as avg_sal
    FROM employees
    GROUP BY department
) as dept_avg;
```

**인덱스 (Index):**
```sql
-- 인덱스 생성
CREATE INDEX idx_employee_name ON employees(name);

-- 복합 인덱스
CREATE INDEX idx_dept_salary ON employees(department, salary);
```

### 4. 데이터 모델링

#### 4.1 ER 다이어그램 (Entity-Relationship Diagram)
```
┌─────────────┐       ┌─────────────┐
│  Department │       │  Employee   │
├─────────────┤       ├─────────────┤
│ PK: id      │ 1   N │ PK: id      │
│    name     │───────│ FK: dept_id │
│    location │       │    name     │
└─────────────┘       │    email    │
                      │    salary   │
                      └─────────────┘
```

**구성 요소:**
- **엔티티 (Entity)**: 사각형 (테이블)
- **속성 (Attribute)**: 타원형 (컬럼)
- **관계 (Relationship)**: 마름모 (연결)

#### 4.2 정규화 (Normalization)
목적: 데이터 중복 최소화, 무결성 향상

**제1정규형 (1NF):**
- 각 속성은 원자값만 가짐
- 반복 그룹 제거

**제2정규형 (2NF):**
- 1NF 만족
- 부분 함수 종속 제거
- 모든 속성이 기본 키에 완전 함수 종속

**제3정규형 (3NF):**
- 2NF 만족
- 이행적 함수 종속 제거

**정규화 예시:**

**비정규화 테이블:**
```
Order (order_id, customer_name, customer_email, product_name, product_price)
```

**3NF 정규화 후:**
```
Customer (customer_id, name, email)
Product (product_id, name, price)
Order (order_id, customer_id, product_id, order_date)
```

#### 4.3 역정규화 (Denormalization)
- 성능 최적화를 위해 의도적으로 정규화 규칙 완화
- 조회 성능 향상
- 데이터 중복 허용
- 주의: 무결성 관리 필요

### 5. 트랜잭션 (Transaction)

#### 5.1 트랜잭션 개념
- **정의**: 논리적 작업 단위
- **예시**: 계좌 이체 (출금 + 입금)

#### 5.2 ACID 특성
- **Atomicity (원자성)**: 모두 실행되거나 모두 취소
- **Consistency (일관성)**: 트랜잭션 전후 데이터 일관성 유지
- **Isolation (격리성)**: 동시 실행되는 트랜잭션 간 독립성
- **Durability (지속성)**: 완료된 트랜잭션은 영구적

#### 5.3 트랜잭션 제어
```sql
-- 트랜잭션 시작
START TRANSACTION;

-- 작업 수행
UPDATE accounts SET balance = balance - 100 WHERE id = 1;
UPDATE accounts SET balance = balance + 100 WHERE id = 2;

-- 성공 시 커밋
COMMIT;

-- 실패 시 롤백
ROLLBACK;
```

#### 5.4 격리 수준 (Isolation Level)
- **READ UNCOMMITTED**: 커밋되지 않은 데이터 읽기 가능
- **READ COMMITTED**: 커밋된 데이터만 읽기
- **REPEATABLE READ**: 동일 트랜잭션 내 일관된 읽기
- **SERIALIZABLE**: 완전한 격리

### 6. 데이터베이스 성능 최적화

#### 6.1 인덱스 (Index)
- **장점**:
  - 조회 속도 향상
  - 정렬 및 그룹화 성능 개선
- **단점**:
  - 저장 공간 필요
  - 삽입/수정/삭제 성능 저하

**인덱스 전략:**
- WHERE 절에 자주 사용되는 컬럼
- JOIN에 사용되는 컬럼
- ORDER BY에 사용되는 컬럼
- 카디널리티가 높은 컬럼 (고유 값이 많은 컬럼)

#### 6.2 쿼리 최적화
```sql
-- 비효율적인 쿼리
SELECT * FROM employees WHERE YEAR(hire_date) = 2024;

-- 최적화된 쿼리
SELECT * FROM employees 
WHERE hire_date >= '2024-01-01' AND hire_date < '2025-01-01';
```

**최적화 기법:**
- SELECT * 대신 필요한 컬럼만 선택
- 인덱스 활용
- 서브쿼리보다 JOIN 선호
- EXPLAIN으로 실행 계획 확인

#### 6.3 파티셔닝 (Partitioning)
- 큰 테이블을 작은 단위로 분할
- 범위, 리스트, 해시, 키 파티셔닝
- 성능 및 관리 용이성 향상

### 7. NoSQL 데이터베이스

#### 7.1 NoSQL 개요
- **Not Only SQL**
- 비관계형 데이터베이스
- 유연한 스키마
- 수평적 확장 (Scale-out)

#### 7.2 NoSQL 종류

**Key-Value Store:**
- Redis, DynamoDB
- 단순한 키-값 쌍
- 캐싱에 적합
```json
"user:1001": {
    "name": "홍길동",
    "email": "hong@email.com"
}
```

**Document Store:**
- MongoDB, Couchbase
- JSON/BSON 문서 저장
```json
{
    "_id": "1001",
    "name": "홍길동",
    "email": "hong@email.com",
    "address": {
        "city": "서울",
        "zipcode": "12345"
    },
    "hobbies": ["독서", "여행"]
}
```

**Column-Family Store:**
- Cassandra, HBase
- 컬럼 기반 저장
- 대용량 데이터 처리

**Graph Database:**
- Neo4j
- 노드와 관계로 구성
- 소셜 네트워크, 추천 시스템

#### 7.3 RDBMS vs NoSQL

| 구분 | RDBMS | NoSQL |
|------|-------|-------|
| 스키마 | 고정 | 유연 |
| 확장 | 수직 (Scale-up) | 수평 (Scale-out) |
| 트랜잭션 | ACID 보장 | BASE (최종 일관성) |
| 복잡한 쿼리 | 지원 | 제한적 |
| 적합한 경우 | 복잡한 관계, 트랜잭션 | 대용량, 빠른 읽기/쓰기 |

#### 7.4 MongoDB 기초
```javascript
// 문서 삽입
db.users.insertOne({
    name: "홍길동",
    email: "hong@email.com",
    age: 30
});

// 문서 조회
db.users.find({ age: { $gte: 25 } });

// 문서 수정
db.users.updateOne(
    { name: "홍길동" },
    { $set: { age: 31 } }
);

// 문서 삭제
db.users.deleteOne({ name: "홍길동" });
```

### 8. 데이터베이스 보안

#### 8.1 접근 제어
```sql
-- 사용자 생성
CREATE USER 'app_user'@'localhost' IDENTIFIED BY 'password';

-- 권한 부여
GRANT SELECT, INSERT, UPDATE ON company_db.* TO 'app_user'@'localhost';

-- 권한 회수
REVOKE INSERT ON company_db.* FROM 'app_user'@'localhost';
```

#### 8.2 보안 모범 사례
- 최소 권한 원칙
- 강력한 비밀번호 정책
- SQL Injection 방지 (Prepared Statement 사용)
- 데이터 암호화 (저장 데이터, 전송 데이터)
- 정기적인 백업

### 9. 백업 및 복구

#### 9.1 백업 전략
- **전체 백업 (Full Backup)**: 모든 데이터
- **증분 백업 (Incremental Backup)**: 변경된 데이터만
- **차등 백업 (Differential Backup)**: 마지막 전체 백업 이후 변경

#### 9.2 복구 계획
- RPO (Recovery Point Objective): 데이터 손실 허용 시간
- RTO (Recovery Time Objective): 복구 목표 시간
- 정기적인 복구 테스트

### 10. PM이 알아야 할 데이터베이스 지식

#### 10.1 데이터베이스 선택 기준
- 데이터 구조 (정형 vs 비정형)
- 트랜잭션 요구사항
- 확장성 요구사항
- 성능 요구사항
- 예산 및 라이선스

#### 10.2 용량 계획
- 초기 데이터량 추정
- 증가율 예측
- 저장소 용량 계획
- 백업 저장소 고려

#### 10.3 데이터베이스 프로젝트 리스크
- 데이터 마이그레이션 복잡성
- 성능 문제
- 데이터 손실 위험
- 스키마 변경의 어려움

## 💡 실습 과제

### 과제 1: 데이터베이스 설계
온라인 쇼핑몰 데이터베이스를 설계하시오:
- ER 다이어그램 작성
- 테이블 정의 (최소 5개)
- 관계 정의 (1:N, N:M)
- 3NF까지 정규화

### 과제 2: SQL 실습
MySQL 또는 PostgreSQL을 설치하고 다음을 수행하시오:
- 데이터베이스 및 테이블 생성
- 샘플 데이터 삽입 (각 테이블 10건 이상)
- 조회, 수정, 삭제 쿼리 작성
- JOIN, 집계 함수 사용 쿼리 작성
- 실행 결과 스크린샷 제출

### 과제 3: RDBMS vs NoSQL 비교
특정 프로젝트 시나리오를 선정하고, RDBMS와 NoSQL 중 어느 것이 적합한지 분석하고 근거를 제시하시오 (A4 1페이지).

## 📖 참고 자료
- "데이터베이스 개론" by Silberschatz
- "SQL 첫걸음" by 아사이 아츠시
- "MongoDB 완벽 가이드" by Kristina Chodorow
- MySQL 공식 문서: https://dev.mysql.com/doc/
- PostgreSQL 공식 문서: https://www.postgresql.org/docs/

## ✅ 자가 점검 퀴즈

1. RDBMS의 주요 특징 3가지는?
2. SQL의 DDL, DML, DCL의 차이는?
3. 정규화의 목적과 1NF, 2NF, 3NF의 조건은?
4. ACID의 의미는?
5. RDBMS와 NoSQL의 주요 차이점 3가지는?

## 📝 핵심 용어
- **RDBMS (Relational Database Management System)**: 관계형 데이터베이스 관리 시스템
- **Primary Key (기본 키)**: 테이블의 각 행을 고유하게 식별하는 키
- **Foreign Key (외래 키)**: 다른 테이블의 기본 키를 참조하는 키
- **정규화 (Normalization)**: 데이터 중복 제거 및 무결성 향상 과정
- **인덱스 (Index)**: 검색 속도를 높이기 위한 자료구조
- **트랜잭션 (Transaction)**: 논리적 작업 단위
- **NoSQL**: 비관계형 데이터베이스

## 다음 주 예고
Week 15에서는 클라우드 컴퓨팅의 개념과 AWS, Azure, GCP의 주요 서비스를 학습합니다.
